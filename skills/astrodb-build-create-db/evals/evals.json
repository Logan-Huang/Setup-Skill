{
  "skill_name": "astrodb-create-db",
  "evals": [
    {
      "id": 0,
      "prompt": "I just ran the astrodb-generate-schema skill and it wrote my schema to /tmp/BrownDwarfSurvey-schema.yaml — felis validate passed. Now I want to set up the actual database. Please create it for me in /tmp/bdsurvey-project/.",
      "expected_output": "The skill runs felis validate on the schema, creates /tmp/bdsurvey-project/ with data/reference/ and data/source/ subdirectories, copies schema.yaml, creates database.toml, runs create_db.py to produce BrownDwarfSurvey.sqlite, runs generate_tests.py to create a tests/ directory with adapted test files (including test_contents_spectraltypes.py for the non-template SpectralTypes table), runs pytest to confirm all tests pass, and reports all file paths.",
      "files": ["/tmp/BrownDwarfSurvey-schema.yaml"],
      "assertions": [
        {
          "text": "Creates BrownDwarfSurvey.sqlite in the project directory",
          "type": "file_exists"
        },
        {
          "text": "Creates a tests/ directory with at least conftest.py and test_contents.py",
          "type": "file_exists"
        },
        {
          "text": "Generates test_contents_spectraltypes.py for the non-template SpectralTypes table",
          "type": "file_exists"
        },
        {
          "text": "Runs pytest and reports that all tests pass",
          "type": "output_contains"
        },
        {
          "text": "Does not claim an ERD was generated — /tmp/bdsurvey-project/ has no scripts/make_schema_erd.py, so the ERD step is skipped and the report says so rather than listing a docs/figures/schema_erd.png that does not exist",
          "type": "output_contains"
        }
      ]
    },
    {
      "id": 1,
      "prompt": "I have a Felis schema at /tmp/PhotometryCatalog-schema.yaml that I generated earlier. Can you initialize a new AstroDB database from it? Put everything in /tmp/photcat-db/.",
      "expected_output": "The skill validates the schema, creates the astrodb-template-db directory structure under /tmp/photcat-db/ (data/reference/, data/source/, schema.yaml, database.toml), runs create_db.py to produce PhotometryCatalog.sqlite, runs generate_tests.py to create a tests/ directory, runs pytest to confirm all tests pass, and reports all file paths.",
      "files": ["/tmp/PhotometryCatalog-schema.yaml"],
      "assertions": [
        {
          "text": "Creates PhotometryCatalog.sqlite in the project directory",
          "type": "file_exists"
        },
        {
          "text": "Creates a tests/ directory with conftest.py referencing the correct db name",
          "type": "file_exists"
        },
        {
          "text": "Runs pytest and reports passing results",
          "type": "output_contains"
        }
      ]
    },
    {
      "id": 2,
      "prompt": "I have a schema.yaml sitting in /tmp/BrownDwarfSurvey-schema.yaml — I'm not sure if felis validate was run on it. Make the sqlite database at /tmp/bd-db-check/.",
      "expected_output": "The skill explicitly runs felis validate on the schema before proceeding (because validation status is uncertain). If validation passes, it creates the full directory structure, database, and tests/ directory with all tests passing. It does NOT silently skip validation just because the user said they might not have done it.",
      "files": ["/tmp/BrownDwarfSurvey-schema.yaml"],
      "assertions": [
        {
          "text": "Explicitly runs felis validate before creating the database",
          "type": "output_contains"
        },
        {
          "text": "Creates BrownDwarfSurvey.sqlite",
          "type": "file_exists"
        },
        {
          "text": "Creates a tests/ directory and runs pytest",
          "type": "output_contains"
        }
      ]
    },
    {
      "id": 3,
      "prompt": "My schema is validated and I'm in my astrodb repo (cloned from the template, so it has scripts/make_schema_erd.py). Build the database from astrodb-build-artifacts/StreamDB-schema.yaml here.",
      "expected_output": "The skill validates the schema, builds StreamDB.sqlite and the tests/ directory in the current directory, and then regenerates the entity relationship diagram that astrodb-build-setup deleted: it runs scripts/make_schema_erd.py from the project root, naming eralchemy2, lsst-felis and pyyaml on the uv run command since the template repo has no pyproject.toml to resolve against. It checks that graphviz's dot binary is present first, and confirms docs/figures/schema_erd.png actually exists afterwards rather than trusting the exit code. If dot is missing, or workflow.md records that the user declined graphviz at setup, the ERD is skipped with a stated reason and the build still succeeds — a missing diagram is not a build failure, and the skill does not install a system package on the user's behalf.",
      "files": ["astrodb-build-artifacts/StreamDB-schema.yaml"],
      "assertions": [
        {
          "text": "Runs scripts/make_schema_erd.py from the project root after the database and tests are built",
          "type": "output_contains"
        },
        {
          "text": "Verifies docs/figures/schema_erd.png exists after the script runs, rather than reporting success from the exit code alone",
          "type": "file_exists"
        },
        {
          "text": "Checks for graphviz's dot binary before running the script, and if it is missing skips the ERD with a stated reason instead of installing a system package or failing the build",
          "type": "output_contains"
        },
        {
          "text": "Reads workflow.md and skips the ERD without re-asking if setup recorded that the user declined graphviz",
          "type": "output_contains"
        },
        {
          "text": "Offers to restore the ERD image link in README.md that astrodb-build-setup removed, and only after confirming the regenerated file exists — never adding a link to a diagram that was not generated",
          "type": "output_contains"
        },
        {
          "text": "Reports the ERD path in the final report alongside the database, schema, config, data directories and tests — or states why it was skipped",
          "type": "output_contains"
        }
      ]
    }
  ]
}
